$('.error_tag').each(function() {
  $(this).html('');
});
<% user.errors.to_hash.each do |field, message| %>
  $('.' + "<%= field.to_s.gsub('.', '_') %>"+ '_txt').parent().find('.error_tag').html("<%= message.join(', ') %>")
<% end %>
